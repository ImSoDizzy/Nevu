# Agent Rules

- Use semantic versioning (MAJOR.MINOR.PATCH) for version numbers and tags. Increment MAJOR for breaking changes, MINOR for new backward-compatible features, and PATCH for backward-compatible fixes.
- Use Conventional Commits for commit messages (e.g., `feat: ...`, `fix: ...`, `chore: ...`).
